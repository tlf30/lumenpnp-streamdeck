#!/usr/bin/env bash
# Elgato HID spec validation test (stop controller first, or use --kill-controller).
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/.venv/bin/python" "$ROOT/log-events.py" "$@"